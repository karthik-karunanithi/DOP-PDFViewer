import React, { useState, useEffect } from "react";

const TestPage = ({ data = null }) => {
  useEffect(() => {}, []);

  return <div style={{ height: "100vh" }}>Is Working</div>;
};

export default TestPage;
